function Test takes nothing returns nothing
    if udg_bool==true then
    endif // endif

    if  udg_bool  ==  true  then
    else
    endif // endif

    if udg_int==0then
    elseif udg_int==1then
    elseif udg_int==2then
    endif // endif

    if  udg_int  ==  0  then
    elseif  udg_int  ==  1  then
    elseif  udg_int  ==  2  then
    else
    endif // endif

    if(udg_bool==true)then
    endif // endif

    if (  udg_bool  ==  true  ) then
    else
    endif // endif

    if(udg_int==0)then
    elseif(udg_int==1)then
    elseif(udg_int==2)then
    endif // endif

    if (  udg_int  ==  0  ) then
    elseif (  udg_int  ==  1  ) then
    elseif (  udg_int  ==  2  ) then
    else
    endif // endif
endfunction